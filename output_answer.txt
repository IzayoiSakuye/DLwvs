测试方法：  
1. 访问链接并观察页面功能，发现可能存在参数`id`的输入点。  
2. 测试基础注入：输入 `?id=1'`，观察是否返回数据库错误（如MySQL、PostgreSQL的语法错误）。  
3. 验证注入类型：输入 `?id=1' -- ` 或 `?id=1' AND 1=1 -- `，若页面正常，则存在字符型注入。  
4. 尝试联合查询：使用 `ORDER BY` 确定列数，例如 `?id=1' ORDER BY 3 -- `，若成功则继续联合注入。  
5. 检查过滤规则：尝试绕过可能的过滤（如`SELECT`被过滤时使用`SELSELECTECT`）。  

预期漏洞：**SQL注入**  
原因：未对用户输入的`id`参数进行有效过滤或参数化处理，导致攻击者可通过构造恶意输入操纵数据库查询逻辑。若测试中观察到错误回显、联合查询结果或盲注响应差异，即可确认漏洞存在。